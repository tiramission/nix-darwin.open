local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 40,
  -- color = colors.bar.bg,
  color = colors.transparent;
  padding_right = 2,
  padding_left = 60,
  position = "bottom",
})
